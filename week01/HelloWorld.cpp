// #include directive is a preprocessor command used to include the contents of a file or library in another file.
// std::cout is used to output data to the standard output, usually the console.
// The << operator is known as the stream insertion operator. It is used to send the data that follows it into std::cout.

#include <iostream>
using namespace std;

int main()
{
    cout << "Hello World!" << endl;

    return 0;
}
